# Normalize line endings across platforms
* text=auto

# Shell scripts must always use LF (so they run on Linux/macOS)
*.sh text eol=lf

# Windows batch files use CRLF
*.bat text eol=crlf

# Treat common binaries correctly
*.png binary
*.jpg binary
*.jpeg binary
*.gif binary
*.pt binary
